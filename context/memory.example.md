# Memory

Copy this file to `context/memory.md` for live project memory.

Do not commit private live memory unless the repository is private and the
content is safe to share.

## Recent Context

- YYYY-MM-DD: Initial harness created from template.

## Decisions

- YYYY-MM-DD: Use `agent.md` as the portable root instruction file.
- YYYY-MM-DD: Keep live state in `context/memory.md` and `context/task.md`.

## Notes For Next Session

- Read `agent.md` first.
- Check `context/task.md` for active work.
- Keep any project-specific preferences in `context/tacit.md`.
